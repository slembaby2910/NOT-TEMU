info:
  name: Login account
  type: http
  seq: 3

http:
  method: POST
  url: https://not-temu.onrender.com/auth/login
  body:
    type: json
    data: |-
      {
        "email": "khoa@test.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
