info:
  name: Check stock
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:8081/inventory/check
  body:
    type: json
    data: |-
      {
        "productId": 1,
        "quantity": 999
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
