info:
  name: Create product
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8081/products
  body:
    type: json
    data: |-
      {
        "name": "Khoa",
        "email": "khoa@test.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​